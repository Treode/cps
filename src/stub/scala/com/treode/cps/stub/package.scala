package com.treode.cps

/** The stub package contains tools to support testing, including single threaded schedulers
  * that schedule tasks predictably and repeatably each run, and sockets that work in process.
  * The stub Scala package lives in the Stub Ivy package.  To make the stubs available to your
  * tests, add the CPS dependency in SBT as follows:
  *
  * '''
  * libraryDependencies += "com.treode" %% "cps" % "0.2.0" % "compile;test->stub"
  * '''
  */
package object stub {}
